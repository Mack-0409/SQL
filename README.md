# SQL
SQL - It is a language which is used to communicate or interact with databae.
SQL [Structured Query Language]

What is Data ?
-> Data is a raw fact that describes attribute of an entity.
                                    [Information]   [Object]

Basic operation peform on the database are....[CRUD]
i.  CREATE / INSERT
ii. READ / RETRIVE
iii.UPDATE / MODIFY
iv. DELETE / DROP

Generally known as "CRUD" operations.

# DBMS [Database Management System]

DBMS is a software we use to maintain and mnage database.
i. Authorization
ii.Authentication

In DBMS we use query language to communicate. It is used to store data in file format.

# RDBMS 

RDBMS - A DBMS software were data will stored int the form of tables.
i. Authentication
ii.Authorization

We can use Structured Query Language [SQL] to communicate.


# Difference between DBMS and RDBMS

                    DBMS                             |                           RDBMS
                                                     |
1. DBMS is a software we used to maintain and manage |  A DBMS  software which allows Relational model 
   the database.                                     |  becomes [RDBMS].
                                                     |
2. We use Query Language                             |  We use Structured Query Language [SQL]. 
                                                     |
3. In DBMS data is stored in the file format.        |  In RDBMS data is stred in the the form of tables.
                                                     |
4. Performing CRUD operationis difficult compared to |  Performing CRUD operation is easy.
   RDBMS.                                            |
                                                     |


# Relational Model 

i. 